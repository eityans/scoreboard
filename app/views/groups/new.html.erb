<h1 class="text-2xl font-bold mb-6">グループ作成</h1>

<div class="bg-white shadow rounded-lg p-6 max-w-md">
  <%= form_with(model: @group, class: "space-y-4") do |form| %>
    <% if @group.errors.any? %>
      <div class="bg-red-100 border border-red-400 text-red-700 px-4 py-3 rounded">
        <% @group.errors.full_messages.each do |message| %>
          <p><%= message %></p>
        <% end %>
      </div>
    <% end %>

    <div>
      <%= form.label :name, "グループ名", class: "block text-sm font-medium text-gray-700 mb-1" %>
      <%= form.text_field :name, class: "w-full border rounded-lg px-3 py-2", placeholder: "例: 金曜ポーカー会" %>
    </div>

    <%= form.submit "作成する", class: "w-full bg-blue-600 text-white py-2 px-4 rounded-lg hover:bg-blue-700 cursor-pointer" %>
  <% end %>
</div>
